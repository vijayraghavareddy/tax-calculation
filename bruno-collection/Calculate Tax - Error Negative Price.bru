meta {
  name: Calculate Tax - Error: Negative Price
  type: http
  seq: 9
}

post {
  url: {{base_url}}/api/v1/calculate-tax
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "address": {
      "street": "123 Main St",
      "city": "New York",
      "state": "NY",
      "country": "US",
      "zipcode": "10001"
    },
    "items": [
      {
        "id": "item1",
        "name": "Product A",
        "price": -100.00,
        "quantity": 1
      }
    ]
  }
}

docs {
  Test error handling when item has negative price.
}

tests {
  test("Status code is 400", function() {
    expect(res.getStatus()).to.equal(400);
  });
  
  test("Error message mentions price", function() {
    const data = res.getBody();
    expect(data.message).to.include("price");
  });
}
